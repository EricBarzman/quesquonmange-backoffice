export type Ingredient = {
  id: number;
  label: string;
  description: string;
}

export type Complexite = {
  id: number;
  created_at: Date;
  label: string;
}

export type Saveur = {
  id: number;
  label: string;
  created_at: Date;
}